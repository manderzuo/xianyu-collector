# 闲鱼库存型分类深度检索记录

日期：2026-09-03

## 结论

当前没有可靠的闲鱼官方 `supportsInventory` 分类字段。系统使用三类证据组合判定：

1. 同一账号的 `mtop.idle.pc.idleitem.editDetail` 实际返回值；
2. 分类推荐接口返回的 `channelCatId`、`tbCatId` 和计价属性；
3. APP 编辑价格面板的实测截图。

已验证库存型组合：`channelCatId=201454708`、`tbCatId=201160807`、计价方式“元/件”，分类名称“设计素材/源文件”。商品 `1080656373759` 的编辑详情返回 `quantity=9998`，并且 APP 价格面板显示库存。

已验证服务履约型组合：`channelCatId=202151841`、分类“AI教学服务”，计价“元/次”，商品 `1081714056697` 的编辑详情虽然返回 `quantity=10000`，但 APP 价格面板不显示库存。

旧版类目 ID 候选：`视频剪辑/制作`（`tbCatId=50050476`）、`广告视频制作`（`tbCatId=201370801`）、`动画制作`（`tbCatId=201157011`）。在本轮同一账号的当前分类属性面板实测中，这三个分类分别只返回按次/按起/按时等服务计价，均没有“元/件”，因此当前版本归入“服务履约”，不再误标为库存候选。代码保留了动态候选规则：只有当前属性响应同时提供“元/件”时才会显示“库存候选”。

## 账号实时样本

账号 8 的商品编辑详情样本：

- `1080656373759`：设计素材/源文件，元/件，quantity=9998，已验证 APP 显示库存。
- `1080691457449`：设计素材/源文件，元/次，quantity=10000，仅验证接口有数量，未验证 APP 展示。
- `1081714056697`：AI教学服务，元/次，quantity=10000，APP 不显示库存。
- `1081662068441`、`1081665220931`：AI教学服务，元/次，接口数量分别为 0、1，属于服务表单。

## 公开资料

- 闲鱼合作方服务端文档列出 `buy_amount`、虚拟商品业务标识和订单状态，并把虚拟发货作为独立能力：[闲鱼合作方-服务端接入文档](https://open.goofish.com/doc/development/dev/server.html)。
- 闲鱼隐私政策确认 APP 存在“发闲置/发服务”发布路径，并可在“我的-我发布的”编辑或下架：[闲鱼社区隐私政策](https://terms.goofish.com/legal-agreement/terms/suit_bu1_taobao/suit_bu1_taobao202103021554_43790.html)。
- 第三方开放平台文档将库存编辑作为独立接口，并说明在架商品的库存会同步到闲鱼 APP；该资料不是闲鱼官方文档，仅作为交叉验证信号：[编辑库存](https://s.apifox.cn/3ac13d69-5a38-4536-ae9b-a54001854ef8/api-93586384)。

## 限制

分类推荐接口对“课程课件资料”“软件 CDKey”等关键词的后续探索请求触发了账号令牌过期，因此未把这些关键词的推荐结果当作已验证事实。对这些内容应在重新登录后，选择平台真实返回的数字内容类目，并确认存在“元/件”后再发布测试商品。
